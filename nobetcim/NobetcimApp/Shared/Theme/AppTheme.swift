import SwiftUI

enum AppTheme {
    static let primary = Color(red: 0.0, green: 0.55, blue: 0.44)
    static let primarySoft = Color(red: 0.87, green: 0.96, blue: 0.93)
    static let call = primary
    static let directions = Color.blue
    static let warning = Color.orange
    static let cardCornerRadius: CGFloat = 16
    static let contentMaxWidth: CGFloat = 860
}
